apiVersion: apps/v1
kind: Deployment
metadata:
  name: tasty-service
  labels:
    app: tasty-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tasty-service
  template:
    metadata:
      labels:
        app: tasty-service
    spec:
      containers:
        - name: tasty-service
          image: tasty-service
          imagePullPolicy: IfNotPresent
          lifecycle:
            preStop:
              exec:
                command: [ "sh", "-c", "sleep 5" ]
          ports:
            - containerPort: 9001
          env:
            - name: BPL_JVM_THREAD_COUNT
              value: "50"
            - name: SPRING_CLOUD_CONFIG_URI
              value: http://config-service
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://restaurant-postgres/restaurantdb_tasty
            - name: SPRING_PROFILES_ACTIVE
              value: testdata
            - name: SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI
              value: http://polar-keycloak/realms/restaurant